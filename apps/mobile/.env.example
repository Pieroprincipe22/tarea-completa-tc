# Copia este archivo a .env y ajusta la URL si hace falta.
# En un móvil físico real, "localhost" no funciona — pon la IP local de tu
# ordenador en la red (ej. http://192.168.1.50:3002).
EXPO_PUBLIC_API_BASE="http://localhost:3002"
